# Read the Docs configuration file
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

python:
  install:
    - method: pip
      path: .
    - method: pip
      packages:
        - sphinx
        - pydata-sphinx-theme
        - matplotlib
        - pandas
        - numpydoc

sphinx:
  configuration: docs/source/conf.py
